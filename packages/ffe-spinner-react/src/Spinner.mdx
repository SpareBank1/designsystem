import { Canvas, Meta, Controls } from '@storybook/blocks';
import * as SpinnerStories from './Spinner.stories.tsx';
import { InstallImport } from '../../../documentation/components/InstallImport';
import dependencies from '../ffe-dependencies';

<Meta of={SpinnerStories} />

# Spinner

Det er mye som skjer i bakgrunnen i et system når innholdet lastes inn. Med spinnere kan vi kommunisere tydelig til brukeren at innholdet er i ferd med å vises snart. Det er best practice å benytte seg av spinnere dersom ventetiden varer lengre enn tre sekunder. Dersom siden bruker mer enn 10 sekunder på å laste inn innholdet, vurder å bruke en notifikasjon med forklarende tekst.

Spinneren finnes i liten og stor versjon. Liten størrelse er default og brukes i de fleste tilfeller. Stor størrelse kan aktiveres ved å sende inn `size="large"`.

<InstallImport
    packageName="@sb1/ffe-spinner-react"
    dependencies={dependencies}
/>

## Forhåndsvisning

<Canvas of={SpinnerStories.Standard} />
<Controls of={SpinnerStories.Standard} />
