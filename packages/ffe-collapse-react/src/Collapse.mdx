import { Canvas, Meta, Controls } from '@storybook/blocks';
import * as CollapseStories from './Collapse.stories.tsx';
import { InstallImport } from '../../../documentation/components/InstallImport';
import dependencies from '../ffe-dependencies';

<Meta of={CollapseStories} />

# Collapse

Komponent for å enkelt lage ekspanderbart område. Hvorvidt området er ekspandert eller ikke styres av konsumenten via property `isOpen`.
Et callback kan gis via property `onRest` som kjøres når transisjonen er ferdig.

<InstallImport
    packageName="@sb1/ffe-collapse-react"
    dependencies={dependencies}
/>

## Forhåndsvisning

<Canvas of={CollapseStories.Standard} />
<Controls of={CollapseStories.Standard} />
