import { Canvas, Meta, Controls } from '@storybook/blocks';
import * as ChipStories from './Chip.stories';
import * as ChipRemovableStories from './ChipRemovable.stories';
import * as ChipSelectableStories from './ChipSelectable.stories';
import { InstallImport } from '../../../documentation/components/InstallImport';
import dependencies from '../ffe-dependencies';

<Meta title="Komponenter/Chips" />

# Chips

Bruk Chips for elementer der brukeren trenger å filtrere eller organisere innhold.

Skriv klare og tydelige labels slik at brukeren forstår formålet med ulike chips.
Om brukeren kan velge chips må de også kunne avvelge chips. Dette er spesielt viktig for multiselect eller filtreringsløsninger.

<InstallImport packageName="@sb1/ffe-chips-react" dependencies={dependencies} />

## Chip

<Canvas of={ChipStories.Standard} />
<Controls of={ChipStories.Standard} />

## ChipRemovable

<Canvas of={ChipRemovableStories.Standard} />
<Controls of={ChipRemovableStories.Standard} />

## ChipSelectable

<Canvas of={ChipSelectableStories.Standard} />
<Controls of={ChipSelectableStories.Standard} />
