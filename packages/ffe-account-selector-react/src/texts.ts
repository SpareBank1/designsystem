const nb = {
    noMatch: 'Ingen samsvarende konto',
} as const;
const nn = {
    noMatch: 'Ingen samsvarande konto',
} as const;
const en = {
    noMatch: 'No matching account',
} as const;

export const texts = { nb, nn, en };
