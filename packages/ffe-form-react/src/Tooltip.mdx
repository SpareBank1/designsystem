import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as TooltipStories from './Tooltip.stories.tsx';

<Meta of={TooltipStories} />

# Tooltip

Om du ikke bruker InputGroup kan du fremdeles bruke deler av den, som f.eks. Tooltip.

<Canvas of={TooltipStories.Standard} />
<Controls of={TooltipStories.Standard}/>

Dersom du bygger din egen hjelpefunksjon og kun trenger knappen så er det ikke nødvendig å sende med innholdet,
du kan bruke onClick prop til å reagere på knappetrykk men da må du også sende med aria-controls som skal
peke på iden til elementet som vises/skjules med knappen din.

<Canvas of={TooltipStories.AriaControls} />

