import { InstallImport } from '../../../documentation/components/InstallImport';
import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as ProgressBarStories from './Progressbar.stories';
import dependencies from '../ffe-dependencies';

<Meta title="Komponenter/Progressbar/Progressbar" />

# Progressbar
Progressbar brukes for å vise fremgangen i en prosess.
Eksempler kan være: opplasting av filer, nedbetaling av lån, gjenværende dager til en gitt dato etc.

## Universell utforming
For å gjøre progressbaren tilgjengelig for skjermlesere, bør man enten legge til ett `aria-label`-attributt som beskriver hva progressbaren viser.
Den vil da lese opp `aria-label - value omregnet til prosent - Progressbar`. F.eks i eksempelet "WithAria", så leser den `nedbetaling av lån 50% prosessindikator`.


<InstallImport
    packageName="@sb1/ffe-progressbar-react"
    dependencies={dependencies}
/>


## Forhåndsvisning
En standard progressbar viser fremdriften i en prosess ved å fylle opp en linje i takt med at prosenten øker. 

<Canvas of={ProgressBarStories.Standard} />
<Controls of={ProgressBarStories.Standard} />

### Success
Noen ganger kan det passe bedre med en grønn progressbar. Da kan du bruke `success`. 

<Canvas of={ProgressBarStories.Success} />
<Controls of={ProgressBarStories.Success} />

## Med Aria-label
For å gjøre progressbaren mer tilgjengelig, kan du legge til et `aria-label`-attributt som beskriver hva fremgangen gjelder. Dette gjør at skjermlesere kan formidle informasjonen til brukere med nedsatt syn. I eksempelet under er det lagt til et `aria-label` for å forklare hva progressbaren viser.

<Canvas of={ProgressBarStories.WithAria} />
<Controls of={ProgressBarStories.WithAria} />
